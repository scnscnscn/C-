#include <stdio.h>
int main()
{
    int x=1,y=2,sum;
    
    for(int n = 3;n <= 11;n++){ 
        sum =x + y;
       = y;
        y  x = sum;
    }
     
     printf("Fibonacci(12) = %d\n", sum);

    return 0;
}